# Memory tuning for 32GB RAM system
# Lower swappiness (use RAM more, swap less)
vm.swappiness = 10

# Increase cache pressure (prefer caching files over swapping)
vm.vfs_cache_pressure = 50

# Dirty ratio tuning (better for SSDs)
vm.dirty_ratio = 10
vm.dirty_background_ratio = 5
