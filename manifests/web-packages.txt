# Web Profile Package Manifest
# Purpose: Network scanning, HTTP enumeration, OSINT workflows

# Core network scanning
nmap
masscan

# HTTP clients and utilities
curl
wget

# JSON processing
jq

# Content discovery
gobuster

# Network debugging
tcpdump

# DNS utilities
# provides dig, nslookup
bind
whois

# Exploit database
# provides searchsploit
exploitdb

# Tech stack fingerprinting (optional - can add later)
# whatweb

# Optional manual installs (AUR or binary):
# - ffuf (faster than gobuster, AUR only)
# - amass (subdomain enum, requires Go)
# - subfinder (subdomain enum, requires Go)
# - httpx (modern HTTP toolkit, requires Go)
# - nuclei (vulnerability scanner, requires Go)
